[package]
name = "soroban-staking-rewards"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk.workspace = true
Sky Moon Scope-math = { path = "../math" }
Sky Moon Scope-error-codes = { path = "../error_codes" }
emergency_guard = { path = "../emergency_guard" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
